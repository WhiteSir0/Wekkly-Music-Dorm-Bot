services:
  music-search:
    build:
      context: .
      dockerfile: Dockerfile.search
    container_name: wekkly-music-search
    restart: unless-stopped
    env_file: .env.search
    ports:
      - "${SEARCH_BIND_ADDRESS:?Tailscale IPv4 required}:4310:4310"
